version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
backend:
  phases:
    build:
      commands:
        - echo 'No build commands'
  cache:
    paths:
      - node_modules/**/*
